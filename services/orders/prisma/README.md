# `prisma/` — orders database

Schema for `daddy_orders`. `Order.cancellationFee` records a late-cancel obligation before escrow exists. `Order.deliveryPhotos` / `deliveryNote` are the daddy's completion evidence (buyer and seller only).
