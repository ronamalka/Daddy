# `tests/` — Vitest

Fast tests that do not open a browser.

- [`unit/`](unit/README.md) — one helper or rule at a time
- [`system/`](system/README.md) — API-level checks with more of the app wired up

Run all of them: `npm test`. Watch mode: `npm run test:watch`.
