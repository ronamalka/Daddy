# `tests/system/`

Broader API tests (`api.test.ts`) that exercise HTTP handlers with more of the stack than a unit test. Includes featured/provider search omitting incomplete daddy profiles, the public service-request teaser, plus sitemap / manifest / per-page metadata checks.
