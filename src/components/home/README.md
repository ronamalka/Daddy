# `home/` — homepage sections

Each file is one block on `/` (hero, stats, categories, featured helpers, testimonials, and so on).

- `index.ts` re-exports the sections
- `data.tsx` — static copy and sample numbers
- `types.ts` — shared prop types
- `results-view.tsx` / `requests-view.tsx` — search results vs local-request list
