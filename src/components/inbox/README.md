# `inbox/` — chat UI

- `messenger.tsx` — conversation list and thread (`MessengerInbox`)
- `new-message-toast.tsx` — small popup when a new message arrives

Photo/PDF attach controls live in `src/components/chat/`.
