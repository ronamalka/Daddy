# `chat/` — message attachments

- `attachment-bubble.tsx` — image or PDF preview in a bubble (allowlisted `/uploads` paths only)
- `composer-attach.tsx` — paperclip, upload via `/api/upload`, pending thumbnail
