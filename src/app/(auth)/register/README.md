# `register/`

The `/register` page. Creates a buyer or seller account and records legal consent. `?role=SELLER` pre-selects helper sign-up; sellers then continue to `/onboarding`. Google sign-up uses the same legal checkboxes, then Auth.js's Google redirect.
