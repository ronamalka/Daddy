# `login/`

The `/login` page. Users sign in with email and password or with Google. A relative `?next=` path is honored after sign-in. Google OAuth errors arrive as `?error=` and are shown in Hebrew.
