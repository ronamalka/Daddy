# `(auth)/` — login pages

These pages share `layout.tsx` (a simple layout without the main shop chrome).

| Folder | Page |
| --- | --- |
| `login/` | Email/password and Google sign-in |
| `register/` | New account |
| `forgot-password/` | Ask for a reset email |
| `reset-password/` | Set a new password from the email link |
