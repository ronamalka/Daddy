# `[id]/`

The `/sellers/:id` page. Public profile of one helper. Ratings are 1–10 (packages and local jobs). `layout.tsx` sets `generateMetadata` and Person / LocalBusiness JSON-LD. The price list can instant-book one visit or start a standing schedule.
