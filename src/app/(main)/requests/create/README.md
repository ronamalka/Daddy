# `create/`

The `/requests/create` page. A buyer posts a local job with city, service, and time window. Creating it notifies matching daddies. An optional unlisted flag hides the post from the public teaser only.
