# `create/`

The `/requests/create` page. A buyer posts a local job with city, optional street and floor, photos (up to 4), a preferred morning/afternoon/weekend window, and a two-hour visit slot. Street is stored but hidden from other daddies until a quote is accepted. Creating it notifies matching daddies. An optional unlisted flag hides the post from the public teaser only.
