# `[id]/`

The `/requests/:id` page. Buyer sees quotes; with two or more priced quotes they compare side by side (price, rating, area overlap, note) before accepting. Sellers can send a price after seeing photos, floor, and preferred window. Street stays hidden until the buyer accepts a quote. Buyer can accept one quote. After accept, the assigned daddy sees the street and **נווט ב-Waze**, plus a link to the job.
