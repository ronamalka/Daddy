# `[id]/`

The `/requests/:id` page. Buyer sees quotes; sellers can send a price after seeing photos, floor, and preferred window. Street stays hidden until the buyer accepts a quote. Buyer can accept one quote.
