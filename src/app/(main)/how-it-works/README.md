# `how-it-works/`

The `/how-it-works` page. Step-by-step explanation of booking a helper, plus an FAQ: unsatisfied users open a dispute from `/orders`, and cancellation is free until 24 hours before the visit (late fee / dispute after work starts — not generic 14-day cooling-off). `layout.tsx` can set extra metadata for this section.
