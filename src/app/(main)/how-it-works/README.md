# `how-it-works/`

The `/how-it-works` page. Step-by-step explanation of booking a helper, plus an FAQ that points unsatisfied users to opening a dispute from `/orders`. `layout.tsx` can set extra metadata for this section.
