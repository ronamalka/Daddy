# `gigs/`

Gig listing API.

| Path | Methods | Purpose |
| --- | --- | --- |
| `/api/gigs` | GET, POST | Search / create (`categoryId` slug or id) |
| `/api/gigs/[id]` | GET, PUT | One gig |
| `/api/gigs/[id]/related` | GET | Similar gigs |
