# `locations/`

`GET /api/locations` — cities and districts for pickers (users service). Serves the users-db city catalog (CBS snapshot, refreshed from data.gov.il in the background once a day). Never waits on the government API.
