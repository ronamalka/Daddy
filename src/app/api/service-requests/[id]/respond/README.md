# `respond/`

`GET /api/service-requests/:id/respond` — quotes on this request, with seller names, ratings, and whether they cover the request city.  
`POST` — seller sends labor, optional materials, and who supplies the parts.
